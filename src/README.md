**`transfer_fact_checking.py`**  
Transfer task fine-tuning script for downstream fact-checking benchmarks. 
See the main [README](../README.md) for usage instructions.

**`requirements.txt`**  
Python dependencies. Install with:
```bash
pip install -r src/requirements.txt
```
